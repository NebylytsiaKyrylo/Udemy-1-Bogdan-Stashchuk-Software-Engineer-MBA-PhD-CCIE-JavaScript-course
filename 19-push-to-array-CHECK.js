/** TASK 19 - Adding elements to an array
 *
 * 1. Create any array
 *
 * 2. Add some new elements to the end of the array
 *
 * 3. Print the length of the resulting array to the console
 */

const myArray = ['Kirill', 7, true];

myArray.push(1, 'Kirill', true); //I will add several elements at once in one line to the end of the array

console.log(myArray);
console.log(myArray.length);
